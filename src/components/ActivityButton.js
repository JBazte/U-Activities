import React from 'react';
import Button from 'react-bootstrap/Button';

function ActivityButton() {
    return (
        <div>
            <Button className='btn btn-primary rounded-1 px-4'>Ver detalles</Button>
        </div>
    )
}

export default ActivityButton;