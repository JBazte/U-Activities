import React from 'react';

function Activity() {
    return (
        //Title, photo, description(modality, category), schedule, location, subscription_button
        <div></div>
    )
}

export default Activity;